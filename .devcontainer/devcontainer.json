{
    "name": "NovaEco Core",
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/workspace",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "dbaeumer.vscode-eslint",
                "ms-azuretools.vscode-docker"
            ]
        }
    },
    "forwardPorts": [
        3000,
        5000,
        8000,
        9000
    ],
    "remoteEnv": {
        "PATH": "${containerEnv:PATH}:/home/appuser/.local/bin"
    },
    "features": {
        "ghcr.io/devcontainers/features/common-utils:2": {
            "installSudo": true,
            "username": "appuser",
            "configureZshAsDefaultShell": true
        }
    },
    // OPTIMIZED COMMAND:
    // 1. Install CLI
    // 2. Install Integration Test Deps (e.g. Faker, requests)
    // 3. Install E2E Test Deps (e.g. pytest-playwright)
    // 4. Install Browsers
    "postCreateCommand": "pip install 'git+https://github.com/novaeco-tech/novaeco-devtools.git@main#subdirectory=novaeco-cli' && pip install -r /workspace/tests/integration/requirements.txt && pip install -r /workspace/tests/e2e/requirements.txt && playwright install && sudo env PYTHONPATH=/home/appuser/.local/lib/python3.11/site-packages /home/appuser/.local/bin/playwright install-deps"
}